version: '3.8'

services:
  skygpt:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./cpa_files:/app/cpa_files
      - ./logs:/app/logs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped